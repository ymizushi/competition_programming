object P26 {
  def main(args: Array[String] = {
      println(combinations(3, List('a, 'b, 'c, 'd, 'e, 'f)))
  }

  // RETRY
  def combinations[A: Ordering](n: Int, l: List[A]): List[List[A]] = {
      
    }
  }
}
